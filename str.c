#include<stdio.h>
#include<conio.h>
#include<string.h>
main(){
     char *p,q[30];
     int i;
     printf("enter the name:");
     //scanf("%s",q);
     gets(q);
     p=q;
     puts(p);
    printf("%s",p);
     getch();
     }
